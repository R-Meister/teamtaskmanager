{
  "name": "team-task-manager",
  "version": "1.0.0",
  "description": "Team Task Manager - Full Stack",
  "scripts": {
    "build": "cd frontend && npm install && npm run build && cd ../backend && npm install && npx prisma generate",
    "start": "cd backend && npm start",
    "postinstall": "cd backend && npm install && npx prisma generate && cd ../frontend && npm install"
  },
  "engines": {
    "node": ">=18.0.0"
  }
}
